For main backend:
Create a python venv.
Run install script for all requirements
Run start api script to load backend (hosts a endpoint with uvicorn)


For our data augmentation steps, refer to the ML model "sub-repos". Checkpoints have not been stored on the repo to avoid LFS.
